# Full audit — mimic track, 2026-09-10

Covers: astro mimic start → panel/categories/x402 detour → now.
Live tracker: `threads.md` (31+ items). Prior audit `AUDIT.md` is a code-era doc, untouched.

## 1. Timeline (what we've been doing)

1. Cloned `prx0r/BEAR`; verified histories (only Timeless truly 2yr).
2. Downloaded/wired 3,683 chart images; built `mimic/` hurdle (activity+direction beat bases).
3. Deployed CF poll worker + VPS scorer; ran live briefly; **paused on order** (1-min polling = 99.65% waste).
4. Thesis → ensemble edges review → per-trader simulator plan (no-LLM verdict).
5. RESEARCH DETOUR (useful): 10-account panel, August quality matrix, shadow set,
   categories + archetypes + scout criteria, canonical schemas v1.0, free-feed map,
   x402 Bazaar recon (competitors + watchlist), Base spend wallet.
6. Back here: astro mock-live trial + July confirmations for graduates.

## 2. Current progress (verified state)

- Models: activity + direction beat base rates OOS (astro/Timeless/XO trials in
  `data/mimic_trials/`); retrieval-text ≈ random (all three) — text stage is THE gap.
- Labels: canonical outcomes complete (450/450); +25 alt labels recovered
  (`all_outcomes_EXPANDED.json`, 485 rows); 234→209 asset-None remain.
- Panel: ~35 accounts measured across 9 archetypes; 7,374 posts classified;
  scout scorecard + thresholds; schemas v1.0 contract.
- Infra: worker deployed but schedules deleted; VPS cron removed; KV frozen;
  221-post pending queue; spend wallet `0x90CE…115` in vault, unfunded.
- Serving: `~/mimichart` blueprint; x402 Tier-1 buys ranked (~$0.02); n0brains bar (61%).

## 3. Money ledger (this session)

`fetch_log.jsonl`: 391 entries, **$0.391** (recon + advanced_search, all $0.001;
`complete` never used). Worker test ticks ≈ $0.015. Total ≈ **$0.41**.
Open gates: backfills ~$1.45 (H6+H12), Tier-1 buys ~$0.02 (H7), GPU $1–3 (H2),
wallet $5 (H4).

## 4. Key findings (numbered, evidence-linked)

- F1 Mock-live: adaptive ≥ frozen everywhere; frozen direction loses to base 2/3 → ship online learning.
- F2 Bridge flat: no direction×regime cell clears 50% lower bound → gate stays STRAT-004-shaped.
- F3 Charts: no return edge (51% vs 55%) but training-necessary (LEAN unlearnable w/o pixels).
- F4 Retrieval-text dead (0.096 vs 0.092) → discriminator then LoRA.
- F5 Poll economics: 1-min = 99.65% waste; timestamps come in-payload; 15-min + dead-zone skip decided.
- F6 Paid-signal market unproven (leaderboard = infra, not signals) → opening + risk.
- F7 n0brains runs our meta-science spec live (61%) → adopt their gate, beat their number.
- F8 Panelists republish gated metrics as text (CQ/STH/ETF) → free proxy before paid APIs.

## 5. Build plan (phases, gates in caps)

- P0 (agent, $0): A1 discriminator → A3 pending labels → A2 brain API → A12 futures wiring.
- P1 ($0): July confirms for JA_Maartun/Frank/Wild/CrypNuevo/AxelAdlerJr → graduate list.
- P2 (HUMAN $): backfills H12 → Tier-1 buys H11 → wallet fund → GPU H2.
- P3: paywall (X1–X3) → resume poll (P1/P2) → delayed-X loop → Bazaar listing.
- P4: AuthorMix transfer, RV head, recursive HL cohorts, stocks gold-set.

## 6. Risks / assumptions register

- R1 Small-n edges (XO n=9, Bheem n=14) must not set weights alone.
- R2 78% bullish / 90% BTC base rates dominate naive models — always beat base.
- R3 Canonical dupe event_ids (10) unfixed; `*_2yr` filenames mislead; only Timeless is 2yr.
- R4 Live infra resumes ONLY on explicit order (pause precedent 2026-09-10).
- R5 Secrets: vault-piped only; pasted R2 token still needs rotation (H0).
- R6 Working trees uncommitted (BEAR + mimichart); commit on human review (H11→L5).

## 7. A-task sprint results (post-audit addendum)

- Discriminator: structural 4-feature SIGNAL (0.73 acc / 0.188 Brier); trigrams HURT
  (0.69/0.299). Voice needs LoRA; shape-filter ships now.
- Market backbone: 5m klines 213k×BTC+ETH (Sep 24→today), funding 2.2k×2, OI/LS
  30d histories; wired into features with graceful fallback. Replay lift ≈ 0
  (honest: live-serving value, not training lift; OI/LS cover 30d only).
- Brain API `:8789` live (health/gate/signal, curl-verified). SVG renderer v0
  (120 candles, levels, 20KB). Schema validator selftest 3/3. Trials re-run stable.
- Blocked A3: strict labeling needs httpx/pandas (box has neither, no pip) —
  either `apt install python3-pip` (H-approval, system change) or wait.

## 8. Zoom-out scorecard

ACHIEVED (verified, on disk): 35 accounts measured · 7,374 posts classified ·
3,871 charts · 485 return labels · 213k-candle 5m histories · activity/direction
models beating bases OOS ×3 · structural discriminator · live-tested poll→score
loop (paused) · local brain API · schemas/contracts/panel/blueprint docs ·
competitor benchmarks + buy rank · $0.41 total spend, fully logged.

MISSING (blocks product): (1) voice generation — retrieval dead, LoRA unfunded;
(2) strict labels for new voters (deps-blocked); (3) full histories (H12 ~$1.39);
(4) public serving (paywall/domain/wallet); (5) live loop (paused); (6) any forward
track record (n0brains has 12,891; we have 0 live).

Critical path: labels → backfills → LoRA → serve → paywall → resume → proof loop.
All six are H/M-gated. A-side is exhausted except maintenance.
