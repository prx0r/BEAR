# HANDOVER 2026-09-10 19:30 UTC — voice pivot, custom extractor, gate inversion

## Where things stand
- **Pivot (user-ordered): voice SHELVED, signals+charts only.** LoRA/AuthorMix-style parked.
  Text = deterministic template (`mimic/post.py`, fail-closed) + Mermaid diagrams
  (`mimic/diagram.py`) + PNG via mermaid.ink (`mimic/render.py`). Full $0 output chain closed.
- **Live processes:** brain API :8789 (healthy), ollama serve :11434 (Hermes 3 8B),
  voice-pilot run IN PROGRESS (terse 0.696 done; astro-verbose failed 0.02 — needs diagnosis).
- **Spend:** ~$1.11 GetXAPI logged. OPENAI_API_KEY vaulted but 401s (needs replacement).
  Apify token rotated+verified. R2 token rotation still open (H0).

## Key results since last handover
1. Custom extractor v1: 8/8 clean (Chase/DrProfit formats) + recovered BEARISH $115k short;
   full guard audit trail in a-logs. Short-context rule added (was bullish-only).
2. Gate-conditioned prove-out KILLED the imported STRAT-004 gate (60%→39% — cut winners).
   Empirical replacement: trade WITH regime (UP+BULL 91%, DOWN+BEAR 80%, fades 8–10%).
3. With-trend OOS test KILLED that too: 51/49 on 318 new labels. No trend edge out-of-sample.
4. Level-trades: model 60–74%, his-direction 70%; $1k-grid finding (75.9%) wired as $500-snap.
5. v1 grid classifier FAILS (constant outputs); swing-nearest remains champion (medAE 1.4%).
6. Pipeline v1 (`mimic/pipeline.py`): 7 stages, hashed receipts, determinism PROVEN
   (identical sha on rerun). 4 handles through stages 0–6.
7. Repo: 17 dead files → stale/ dirs, README banner, AGENTS paths fixed, RECIPES.md,
   threads deduped (A1–A18/H0–H14), branches seed0→seed1.4 pushed.

## Open gates (all need explicit human go)
- H6 backfills ~$0.45–1.45 · H11 Tier-1 x402 buys ~$0.02 · H4 fund $5 wallet ·
  H1 resume live · H2 LoRA $1–3 · apt chromium (PNG) / apt pip (A3) system changes.

## Next agent: read threads.md → TASKSPEC.md → this file. Continue voice pilot first.
