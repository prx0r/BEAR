# Peer review — full pipeline (2026-09-10, branches seed0→seed1.4)

Reviewer job: attack every numbered claim. Each carries its evidence pointer and
repro command ($0, offline). Confirm or send back with the discrepancy.

## Claims inventory

**Models**
- C1 Activity models beat base OOS (astro 0.147/0.150, Timeless 0.048/0.072, XO 0.033/0.035).
  Ev: `data/mimic_trials/*_seed1.4_*.json`. Repro: `./mimic/trial.sh HANDLE`.
- C2 Direction models beat base live-adaptive but FAIL hard gates on secret
  (astro frozen worse than base; Timeless secret worse; XO n<100).
  Ev: same reports + `GATE` lines. Implication: no direction product yet.
- C3 Structural discriminator 0.73 acc (4 feats); trigrams HURT (0.69/0.30 Brier).
  Ev: `discriminator_seed1.json`. Attack surface: n=690, single split.
- C4 `swing_pos` debuted #2 weight; direction replay best 0.1537 (was 0.1574).
  Ev: replay stdout in a-logs. Small delta — could be noise.
- C5 Rerank gate correctly refuses everywhere (val rr<k1 all handles).
  Ev: trial stdout. Note: contradicts earlier Timeless-only win — window effect.

**Labels & data**
- C6 508 outcomes (460 canonical + 48 recovered, 10 dupes removed post-hoc).
  Ev: `all_outcomes_EXPANDED.json`. Attack: recovery protocol differs (daily entry).
- C7 Custom extractor 8/8 clean by inspection (guard audit in a-logs).
  Attack: inspection is mine — re-eyeball the 8 + hunt false negatives
  (recall unknown; BEARISH $115k short-range hit was LOST in guard tightening).
- C8 Round-trips: 82 chains, partial fracs 33% set / 67% null (no imputation — good).
- C9 Levels: 2,077 TIER1; swing medAE 1.4%; 75.9% on $1k grid; v1 grid FAILS.
- C10 Prove-outs: model≈man everywhere, no CI clears 50% except astro level-trades
  60% (51–68) and his-direction+levels 70% (62–78).

**Pipeline integrity (self-flagged)**
- F1 Trial reports silently not-written for ~1h (NameError swallowed by grep
  habit). Fixed; but any numbers reported 13:00–14:00 without a file are suspect.
- F2 String-sorted timestamps bit twice (mocklive fixed; roundtrips fixed).
  Audit any other `sorted(key=createdAt)` — grep it.
- F3 Workdir slips (`-m` run from repo root) caused 3+ phantom errors; harmless
  but slowed loops. RECIPES documents correct cwd per command — verify it does.
- F4 Threads C-log needed 3 repair commits (dup numbering). Process fix: append
  via script, not hand-edits.
- F5 XO gap window 05-11→25: pulled +181 AFTER initial +0 — early "empty" verdicts
  on sparse windows may be wrong; re-check 52kskew/Husslin_ empties the same way.
- F6 APIFY_TOKEN + OPENAI_API_KEY handling: pasted secrets transit tool calls;
  OPENAI key 401s (needs replacement); R2 token still unrotated (H0).

## Open questions for reviewer

1. Is n=53–55 direction-secret enough to FAIL, or should the gate be
   pooled-cross-handle instead of per-handle?
2. Does the 70% his-direction+levels number survive transaction costs + slippage?
   (No cost model in loop — n0brains downgrades these; we don't. Yet.)
3. strict_NEW/outcomes_NEW live beside canonicals — merge or keep split forever?
4. Brain warmup 5.5 min — acceptable, or cache weights (N-proposal #7)?

## Verdict requested

PASS (proceed to backfills H6) / CONDITIONAL (fix listed items) / FAIL (state why).
