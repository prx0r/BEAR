# A-log 2026-09-10 13:10 UTC — env upgrade: pip/venv, API key, Hermes

## seed0 review
- Ours: 6-line frozen reference (`mimic/seed0.json`, branch `seed0`). Usage: every
  future seed compares against it, never tuned, never re-run for decisions.
- cg repo: NO seed0 concept found (grep docs+SPEC+AGENTS empty). Our convention stands.

## pip (no pip/ensurepip on box; passwordless sudo present)
- `apt install python3-pip python3-venv`, then venv `~/.venvs/bear` (NOT
  --break-system-packages): pydantic 2.13.5, httpx 0.28.1, pandas 3.0.5.
- Unblocks: backtest.py/price_data imports, real DataFrames, API clients.

## API key (user-pasted sk-...)
- Stored as `OPENAI_API_KEY` in oracle vault (length-verified, temp file shredded).
- VALIDATION FAILED: `GET /v1/models` → 401 incorrect-key. Key never displayed
  anywhere by agent. H-TASK: verify in OpenAI dashboard / send replacement.

## opencode-go recon ("conhardset" interpretation)
- No opencode HTTP endpoint exists (only CLI sessions; ports show brain/vault/ssh).
- `opencode.jsonc` is `{}` — nothing to hard-set against yet.
- Wrote `~/.config/opencode/hermes-local.example.jsonc` (NOT auto-loaded):
  merge to route bulk work to local Hermes, keep cloud default for planning.
- "muse-spark-1.3-contributor" is a cloud model id, not servable locally.

## Hermes (NousResearch Hermes 3 8B via ollama, CPU-only, no GPU on box)
- `ollama serve` on :11434 (OpenAI-compatible), model pulled (4.7GB).
- Smoke test: "HERMES ONLINE" ✓ (31s cold, ~5GB RAM resident).
- Use: bulk paraphrase (AuthorMix neutrals), style eval batches, $0.
