# A-log 2026-09-10 10:30 UTC — round-trips + 30d labels

## Commands
- `python3 -m mimic.roundtrips astronomer_zero` (fixed `load_prices` 2-tuple bug first)
- 30d labels via stdlib from 1h closes (fixed: outcomes carry NO timestamps —
  joined `published_at` via events by `event_id`)
- Sample-trip inspection (JSON read)

## Results
- Round-trips: 32 from 766 posts, 15 partials linked, 17/32 win (53%),
  avg -1.19%, avg hold 133h. Structure validated on narrative chains
  (entry "TP my longs, 10% runners" → partial → flip "flipped shorts into longs").
- 30d labels: n=448, 52% win (`data/outcomes_30d.json`). Month horizon = coin flip,
  same as 4h/24h/7d. No horizon rescues raw direction.
- Files: `mimic/roundtrips.py`, `data/roundtrips_astronomer_zero.json`,
  `data/outcomes_30d.json`.

## Bugs fixed
1. `load_prices` returned `(list, list)` 2-tuple; `px_next` unpacked rows → ValueError.
2. Outcomes have no timestamps (neither canonical nor EXPANDED) — join via events.

## Open (not blocked, next loop)
- Round-trip the other 4 handles (same command, new handle arg).
- Partial-fraction parsing ("closed 45%" → 0.45 works; "just under half" → null, needs word-list).
- Chart-anchored entries (needs H: apt imaging tools, or M: VLM).
