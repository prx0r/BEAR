{
 "seed": "seed1.4",
 "from": "seed1.3",
 "justification": "seed1.3 frozen still fails under drift (train prior != mock mix). Instead of better priors, calibrate OUTPUTS: train-fitted intercept (frozen) + trailing-20 recalibration (live). Tests whether miscalibration, not signal, is the failure.",
 "changes": [
  "adaptive bias: frozen intercept refit on train; live trailing-20 residual correction",
  "gate evaluated on corrected copies (originals kept as comparators)"
 ],
 "frozen_rest": [
  "handles",
  "split",
  "priors",
  "features",
  "retrieval",
  "protocol"
 ]
}