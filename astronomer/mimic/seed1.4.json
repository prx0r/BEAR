{
  "seed": "seed1.4",
  "from": "seed1.3",
  "justification": "Per-handle rerank gate (rerank helped Timeless/Bheem only — gate on validation, default off); custom extractor v1 for Chase/DrProfit formats (8/8 clean); strict_NEW merged into prove-out sources; full-history trials (XO 4536/Bheem 1638 posts).",
  "changes": ["rerank gated per-handle on val window (use_rerank flag)", "custom_rules.py v1", "proveout reads strict_NEW + outcomes_NEW", "trial reports carry use_rerank diagnostics"],
  "frozen_rest": ["priors", "features", "protocol", "gates"]
}
